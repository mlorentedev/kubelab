services:
  web:
    image: "${REGISTRY-docker.io/mlorentedev}/web:${TAG:-latest}"
    container_name: web
    restart: always
    expose:
      - ${PORT}  
    env_file:
      - ./.env
    networks:
      - proxy

networks:
  proxy:
    external: true
