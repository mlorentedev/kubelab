services:
  web:
    image: "${REGISTRY}/${IMAGE_NAME}:latest"
    container_name: ${CONTAINER_NAME}
    restart: always
    expose:
      - ${PORT}  
    env_file:
      - ./.env
    networks:
      - proxy

networks:
  proxy:
    external: true
