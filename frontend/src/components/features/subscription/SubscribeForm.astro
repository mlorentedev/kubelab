---
const { tag = '', utm_source = 'landing', button_text = '📩 VALE' } = Astro.props;
const tagsString = Array.isArray(tag) ? tag.join(',') : tag;
const endPoint = import.meta.env.PUBLIC_BACKEND_URL + '/api/subscribe';
---

<div class="bg-cyan-700 text-white p-4 rounded-md max-w-xs mx-auto my-4">
  <form 
    class="flex flex-col gap-6"
    hx-post={endPoint}
    hx-target="#status-message"
    hx-target-409="#status-message"
    hx-trigger="submit"
    hx-swap="innerHTML"
    hx-swap-oob="true"
    hx-include="[name='email'], [name='utm_source'], [name='tag']"
  >
    {tag && <input type="hidden" name="tag" value={tagsString} />}
    <input type="hidden" name="utm_source" value={utm_source} />

    <input
      type="email"
      name="email"
      placeholder="Tu correo electrónico"
      required
      class="w-full p-2 text-cyan-700 rounded-md text-sm focus:outline-none focus:ring-1 focus:ring-cyan-300"
    />
    <button
      type="submit"
      class="w-full px-4 py-1.5 bg-white text-cyan-700 rounded-md text-sm font-medium hover:bg-cyan-100 transition-colors"
    >
      {button_text}
    </button>
    <div class="flex justify-center items-center text-center">
      <input type="checkbox" id="privacy-policy" name="privacy-policy" class="mr-2" required />
      <label for="privacy-policy" class="text-xs">
        Acepto la <a href="/privacy-policy" target="_blank" class="text-white"
          >política de privacidad</a
        >
      </label>
    </div>
  </form>
  <div id="status-message" class="mt-4 text-xs text-center"></div>
</div>