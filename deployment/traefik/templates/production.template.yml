http:
  routers:
    production:
      rule: "Host(`{{PRODUCTION_DOMAIN}}`)"
      entryPoints:
        - {{PRODUCTION_ENTRYPOINT}}
      service: {{PRODUCTION_SERVICE}}
      tls:
        certResolver: {{CERT_RESOLVER}}
      middlewares:
        - secure-headers@file
        - rate-limit@file

  services:
    {{PRODUCTION_SERVICE}}:
      loadBalancer:
        servers:
          - url: "http://{{PRODUCTION_SERVER}}:{{PRODUCTION_PORT}}"