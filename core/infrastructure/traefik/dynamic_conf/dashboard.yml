
http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.mlorente.dev`)"
      entryPoints:
        - websecure
      service: api@internal
      tls:
        certResolver: myresolver
      middlewares:
        - auth
        - secure-headers