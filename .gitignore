# Project artifacts
_site/
.sass-cache/
.vagrant/
.tmp/
.bckp/
dist/
bin/
output/

# Build artifacts
*.pyc
__pycache__/
*.py[cod]
*.so
*.dylib
*.dll
*.exe
*.out
*.test
*.tmp

# Dependencies
node_modules/
.venv/
.venv*/
venv/
vendor/

# IDE & Editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
ehthumbs.db
*.bak
.cache/

# Archives (old code)
.archive/

# Testing & Coverage
htmlcov/
.coverage
.coverage.*
coverage.xml
.pytest_cache/
*.cover
*.prof

# Type checking & Linting
.mypy_cache/
.ruff_cache/
.tox/

# Poetry
poetry.lock

# Ruby / Jekyll
Gemfile.lock
beautiful-jekyll-theme-*.gem
*.jekyll-cache

# Drawio
*.bkp

# Environment files — ignore real values, keep examples
.env
.env.local
.env.dev
.env.staging
.env.prod
.env.*
!.env.example
!.env.*.example
infra/config/env/.env.*
!infra/config/env/.env.*.example

# Secrets & Certificates (includes Authelia JWKS keys)
certbot/
certs/
*.pem
*.key
*.crt
*.p12
*.pfx

# Logs
logs/
*.log

# AI assistants
.claude/
.chat/
AGENTS.md
GEMINI.md
MEMORY.md

# Uptime Kuma backups (binary SQLite, use `toolkit monitoring backup` to pull)
infra/config/uptime-kuma/*.db

# Docker overrides
docker-compose.override.yml

# Docker-created artifacts (volume mount directories)
# Prevent accidental directory creation by Docker when volume sources don't exist
apps/**/.env.*/
apps/**/loki/
apps/**/vector.toml/
apps/**/nginx.conf/

# Wiki build output
apps/wiki/generated_docs

# K8s overlay generated manifests — committed for GitOps (ArgoCD reads from Git).
# Regenerate with: make deploy-k8s ENV=<env> (or toolkit infra k8s deploy --env <env>)
# Previously gitignored, unignored for ARGO-007.

# Security scanning
.cache_ggshield
