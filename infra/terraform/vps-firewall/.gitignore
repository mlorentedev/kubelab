# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl

# Generated from common.yaml by `toolkit infra terraform vps-firewall-tfvars`.
# Never hand-edited, never committed — the SSOT is networking.firewall.vps_inbound.
*.tfvars
