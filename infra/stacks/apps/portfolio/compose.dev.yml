services:
  portfolio:
    build:
      target: build
    command: ["npx", "astro", "dev", "--host", "0.0.0.0", "--port", "8080"]
    volumes:
      - ../../../../apps/web/portfolio:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
