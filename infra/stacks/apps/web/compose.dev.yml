services:
  web:
    build:
      target: build
    command: ["npm", "run", "dev", "--", "--host"]
    volumes:
      - ../../../../apps/web/astro-site:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
