# Managed by Ansible — do not edit manually
http:
  middlewares:
    error-pages:
      errors:
        status:
          - "404"
        service: nginx
        query: "/404.html"

  services:
    nginx:
      loadBalancer:
        servers:
          - url: "{{ traefik_error_pages_backend }}"
