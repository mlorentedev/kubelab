# Managed by Ansible (dev_node role, ANSIBLE-028) — do not edit by hand.
# Reproducible toolchain SSOT: `mise install` materializes exactly these.
[tools]
node = "{{ dev_node_toolchains.node }}"
go = "{{ dev_node_toolchains.go }}"
python = "{{ dev_node_toolchains.python }}"
