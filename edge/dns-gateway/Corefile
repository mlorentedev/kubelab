staging.kubelab.live {
    hosts {
        # MiniPC B Tailscale IP — replace with actual IP after Tailscale setup
        # <minipc-tailscale-ip>  staging.kubelab.live
        # <minipc-tailscale-ip>  *.staging.kubelab.live
        fallthrough
    }
    log
    errors
}

. {
    forward . 1.1.1.1 8.8.8.8
    cache 30
    log
    errors
}
