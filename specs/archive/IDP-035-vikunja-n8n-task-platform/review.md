---
spec: "IDP-035-vikunja-n8n-task-platform"
verdict: "PASS-WITH-GAPS"
reviewed_sha: "f637d556f681569c491eb94a7e9d0fc5e5e696ca"
reviewer: "nan/deepseek-v4-flash"
date: "2026-08-27"
---

## Adversarial review

**Scope**: IDP-035-vikunja-n8n-task-platform (branch `feat/idp-035-vikunja-task-platform`, HEAD `f637d556`)
**Sources**: `specs/IDP-035-vikunja-n8n-task-platform/{proposal,tasks,verification,features}.json`, `infra/n8n/workflows/{multi-forge-sync,slack-task-capture,agent-dispatcher}.json`, `infra/k8s/base/services/{vikunja,n8n}.yaml`, `infra/config/values/{common,staging,prod}.yaml`, `infra/config/secrets/{staging,prod}.enc.yaml` (key presence, values not decrypted), `toolkit/features/{k8s_secrets,secrets_manager,postgres_provisioner,vikunja_client,vikunja_reconciler}.py`, `toolkit/cli/infra.py`, `tests/test_k8s_secrets_apply.py`, `tests/test_n8n_{multi_forge_sync,slack_capture,agent_dispatcher,import}.py`, `tests/test_vikunja_{client,reconciler}.py`, `tests/test_postgres_provisioner.py`, `docs/adr/adr-066-self-hosted-task-platform.md`, `docs/runbooks/vikunja-task-platform.md`.

This review is performed at `f637d556`, **one commit past** the prior FAIL review (`08cc8d3f`). That single commit (`feat(n8n): enable rawBody options, restore fail-closed secrets, and populate SOPS machine catalog`) is, on its face, a direct response to the prior review's two REAL blockers. I re-verified each claimed fix by **running** the suite, linters, typechecker, SOPS key presence, manifest render, and **mutation edits against the live shipped workflow JS** (reverted afterwards). **The two code-level blockers are genuinely resolved and test-proven; the remaining gaps are verification/evidence and theoretical — none of them a REAL Blocker.** Verdict: **PASS WITH GAPS**, not FAIL.

### Spec and task alignment

- The n8n tests execute the **real shipped `jsCode`** (`eval_forge_node` / slack equivalents read the workflow JSON). My mutations modified that JSON directly and the negative tests caught it — proving the ships-as-tested contract is real.
- **Prior Blocker A (Raw Body) — RESOLVED**: both Webhook Ingress nodes now set `options: { "rawBody": true }` (`multi-forge-sync.json` `b2000000…001`, `slack-task-capture.json` `c3000000…001`). The parse-code nodes now read the raw string from `$json.rawBody` first, then `$json.body` as a string, and only fall back to re-serializing a parsed object. With `rawBody: true`, n8n exposes the exact request bytes; the code verifies the HMAC over those bytes rather than over re-serialized JSON. A structural test gates the option: `test_multi_forge_workflow_json_structure` and `test_slack_workflow_json_structure` each assert `webhook_node["parameters"]["options"]["rawBody"] is True`.
- **Prior Blocker B (fail-closed required secrets) — RESOLVED**: `vikunja-secrets` restored `VIKUNJA_DATABASE_PASSWORD`, `VIKUNJA_AUTH_OPENID_CLIENTSECRET`, `VIKUNJA_SERVICE_JWTSECRET` to required `keys` (only `VIKUNJA_FILES_S3_SECRETACCESSKEY` remains optional). `_apply_single_secret` fails closed on any missing required key (`missing` → refuse, no partial apply), pinned by `test_k8s_secrets_apply.py` (`test_partial_mapping_fails_closed_without_calling_kubectl`, `test_all_missing_fails_closed_without_calling_kubectl`). `k8s_provision_postgres_tenant` was reverted from `warning + return` back to `raise typer.Exit(1)` on a missing `db_password`, so `make deploy-k8s` no longer reports green for a non-provisionable tenant.
- **Prior Finding C (empty SOPS) — ADDRESSED**: both `staging.enc.yaml` and `prod.enc.yaml` now carry the Vikunja group (`vikunja`), the n8n webhook/API-token group (`vikunja_api_token`, `forge_webhook_secret`, `slack_signing_secret`), the Authelia Vikunja OIDC secret, and its Argon2 hash. `SECRET_CATALOG` declares three n8n `SecretSpec`s; `n8n.yaml` reads the three distinct webhook secrets from `n8n-secrets` (no aliasing back onto the Vikunja JWT). I confirmed key presence without decrypting/printing values.
- **Regime note**: spec `status: implementing`; the closing task "PRs merged deliberately under trunk-based workflow" remains unticked; no PR is open; `verification.md` evidences render + dry-run apply only, with no live-deployment output. The verification window is not cleanly reached, but the review is performed as instructed.

### Findings

| Severity | Reality | Area | Finding | Evidence | Test (named, or UNTESTED) | Fix location (code / tests / spec / vault) |
|----------|---------|------|---------|----------|---------------------------|---------------------------------------------|
| Minor | REAL | AC1/AC4/AC5 (verification) | No live-deployment / E2E evidence exists. `verification.md` proves only that `kubectl kustomize` renders (stateless Deployment replicas:1, no PVC, 128Mi/256Mi, `VIKUNJA_DATABASE_MAXOPENCONNECTIONS: 20`, R2 endpoint configured) plus dry-run secret apply. There is no running pod, no demonstrated connection to `infra.postgres`, no R2 write, and no live acceptance of a genuinely GitHub/Gitea/Slack-signed request. AC1 "runs and connects; restart preserves data" and AC4/AC5 "real senders" are asserted from renders and self-consistent fixtures, not observed behavior. Spec is `implementing`, no PR open, closing task unticked. | re-ran `kubectl kustomize infra/k8s/overlays/{staging,prod}` (renders); parsed the vikunja Deployment via yaml (volumes: [], hasPVC: False, replicas 1); grepped `verification.md` for live outputs (none). | n/a — no live test artifact. UNTESTED at E2E level. | spec (deploy to staging/prod, capture real pod/DB/R2 evidence, then verify) |
| Minor | THEORETICAL | AC4 HMAC (non-raw fallback) | The object fallback path still computes the HMAC over `JSON.stringify(body)` when no raw string is present (key/name ordering, number formatting, escaping vs. what the sender signed). This is now genuinely mitigated because `rawBody: true` guarantees n8n delivers the exact bytes and the code prioritizes that branch; the residual risk exists only if n8n fails to provide any raw string. The object-path unit tests are order-coupled (Python `json.dumps(...,separators=(',',':'))` must equal Node `JSON.stringify` output). | code read of forge `jsCode` branches; the valid-HMAC object test passes only because both sides compact-serialize in the same key order. | `test_workflow_node_valid_hmac_and_open_pr` (object path), `test_workflow_node_raw_body_string_and_valid_hmac` (raw path). UNTESTED for a non-canonical (reordered) external payload on the fallback path. | tests (add a fixture that reorders keys / uses non-ASCII on the object fallback) |
| Minor | SPECULATIVE | AC5 Slack | Slack signature is correct only if n8n delivers the exact raw body bytes; the code covers both `rawBody`-string and `body`-string branches and re-encodes from an object only as a last resort (order-insensitive for `urlencoded`, low risk). No evidence of a live failure; theoretical only. | code read of slack `jsCode` branches. | `test_slack_workflow_node_raw_form_urlencoded_signature`, `test_slack_workflow_node_expired_timestamp_fails_closed` (real JS). | — (surface; verify at deploy) |
| Minor | REAL | AC1/AC2 (R2) | `VIKUNJA_FILES_S3_SECRETACCESSKEY` is in `optional_keys`, so Vikunja can be deployed green without R2 write auth even though the proposal requires Cloudflare R2 for file uploads/avatars. Not a security fail-closed regression (the three required secrets are enforced), but AC1's R2 availability is not fail-closed. SOPS now carries the `vikunja` group, so in practice the key is expected present — the residual is that nothing *refuses* if it is absent. | code read of `k8s_secrets.py` vikunja mapping (S3 key optional); SOPS key presence (not value). | no named test asserts S3 key present-or-refuse. UNTESTED. | code (optionally promote S3 key to required) / spec |
| Minor | REAL | AC3 provisioner | The missing-`db_password` fail-closed branch (`raise typer.Exit(1)`) is not directly unit-tested; `test_postgres_provisioner.py` covers only `build_provision_sql`. Low risk (single guard already reverted to hard-fail and manually verified via diff), but the specific failure path is UNTESTED. | read `toolkit/cli/infra.py` `k8s_provision_postgres_tenant` (error + `raise typer.Exit(1)`); only `build_provision_sql` is tested. | no named test for the Exit(1) path. UNTESTED. | tests (add a test asserting provisioning refuses without the password) |

### What is verified and healthy (not rubber-stamped)

- **Mutation-proven fail-closed HMAC**: I temporarily replaced the shipped forge JS `isValidSig = (sig === …)` assignment with `isValidSig = true;` and the slack JS `isValidSlack = (slackSig === computed);` with `isValidSlack = true;`, re-ran the negative tests, and both `test_workflow_node_invalid_hmac_fails_closed` and `test_slack_workflow_node_invalid_signature_fails_closed` **failed** — proving the enforcement in the live JS is genuine. (In a first, weaker mutation that only flipped the initializer, the missing-signature test caught it but the invalid-signature test did not, because it is overwritten by the real comparison — consistent with real enforcement.) Reverted; tree clean apart from review files.
- **rawBody structurally gated**: the option is asserted `True` in both workflows by `test_multi_forge_workflow_json_structure` / `test_slack_workflow_json_structure`.
- **Fail-closed required secrets pinned**: `test_k8s_secrets_apply.py` pins the TOOL-018 refuse-partial behavior; `vikunja-secrets` required keys restored.
- **Full suite green**: `poetry run pytest` on the 9 relevant files → **93 passed** in 9.4s; `mypy` clean on 6 touched toolkit modules; `ruff` clean.
- **Provisioner wiring**: `make deploy-k8s` chains apply → provision → validate; no placeholder password.
- **Stateless render**: parsed the vikunja Deployment — `replicas: 1`, `volumes: []`, `hasPVC: False`, `requests/limits` 128Mi/256Mi + 0.1/0.5, `envFrom` `vikunja-config` + `vikunja-secrets`, `VIKUNJA_DATABASE_MAXOPENCONNECTIONS: "20"`, R2 endpoint `https://r2.cloudflarestorage.com` configured.
- **SOPS populated** (keys present, values encrypted) for `vikunja`, n8n webhook/API secrets, and the Vikunja OIDC secret in both staging and prod — key presence confirmed without exposing plaintext.

### Evaluator rubric

| Dimension | Grade (A-D) | Rationale (one line) |
|-----------|-------------|----------------------|
| Correctness        | C | Unit + mutation-proven logic and stateless render are correct, but AC1/AC4/AC5 live acceptance (pod-connected DB/R2, real-sender HMAC) is unproven — evidence is render/unit level only. |
| Verification       | C | Strong real-JS unit tests, structure-gated rawBody, mutation-proven negatives, mypy/ruff clean, SOPS presence checked; but no live-deploy/E2E output and fixtures remain self-consistent. |
| Scope              | B | Diff matches the proposal's files; the required-secret relaxation from the prior review was **reverted** (not kept as creep); side-changes documented. |
| Reliability        | B | Fail-closed required secrets and hard-fail provisioner restored and tested; residual theoretical serialization-order risk only on the non-raw fallback; R2 not fail-closed. |
| Maintainability    | B | Clear typed Python client/reconciler, lint/mypy clean; dual-branch raw-body handling and static-JSON workflows are acceptable, mild smells (dead-code risk low since option is now on). |
| Handoff-readiness  | C | ADR-066 + runbook exist (AC7) and SOPS is populated, but AC1/AC4/AC5 are documented as delivered without a live deploy or live-sender acceptance; no PR merged, spec `implementing`. |

### Verdict
**PASS WITH GAPS** — the two code-level REAL blockers from the prior FAIL are **resolved and test-proven**: Raw-Body is enabled and structurally gated with byte-correct HMAC code, and fail-closed required secrets + a hard-fail provisioner are restored and pinned by tests. No Blocker and no Major remains. What stands between this and a clean PASS is evidence and edge-hardening, all tracked as Minors: (1) no live deployment/E2E proof for AC1/AC4/AC5, (2) the theoretical object-fallback serialization-order HMAC fragility, (3) R2 access key left optional, (4) the provisioner Exit(1) path untested by a named unit test. Rubric has C's (Correctness, Verification, Handoff) and no D → PASS WITH GAPS is the mechanical outcome.

### Recommended next steps (before archive)
1. **Deploy live and capture evidence**: run `make deploy-k8s ENV=staging` + `apply-secrets`, record the pod `Running`, a DB connection check against `infra.postgres` database `vikunja`, and an R2 write — real command output appended to `verification.md`. This resolves the dominant AC1/AC4/AC5 evidence gap.
2. **Prove real-sender acceptance at least once**: feed an actually external-signed fixture (real GitHub/Gitea/Slack signature over a real raw body) through the workflow JS, or better, hit the live n8n webhook, and record it. This moves the AC4/AC5 claim from self-consistent to externally-proven.
3. **Add tests for the two untested paths**: a named test for `k8s_provision_postgres_tenant` refusing when `db_password` is missing (Exit(1)), and a non-canonical (reordered/non-ASCII) external signature on the forge object fallback path to bound the theoretical risk.
4. **Decide R2 fail-closed**: either promote `VIKUNJA_FILES_S3_SECRETACCESSKEY` to required `keys`, or document the deliberate optionality in ADR-066/runbook.
5. **Reconcile docs**: have ADR-066/runbook state that delivery is proven at render/unit level with SOPS populated, and that live deploy + real-sender acceptance are the remaining verification-window items.
6. **Complete the workflow**: merge the PRs under trunk-based flow, tick the closing task, move spec `implementing → verifying → archived`, then run one final review at the merged sha. `dotf spec archive` should be invoked only after steps 1–6.

`dotf spec archive` is **not advisable in the current state**: PASS WITH GAPS with AC1/AC4/AC5 unmet by live evidence, the spec still `implementing`, the closing "PRs merged" task unticked, and no PR open. Deploy, verify live, merge, then archive.
