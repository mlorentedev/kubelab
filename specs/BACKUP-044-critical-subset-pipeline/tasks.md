---
tags: [spec, tasks]
created: "2026-08-15"
---

# Tasks - BACKUP-044-critical-subset-pipeline

> TDD order. One task = one focused commit. Tick as you go. Reorder freely while spec is in `draft` state; freeze once you start `implementing`.
>
> **Inline markers:**
> - `[P]` — no dependency on another unchecked task, safe to run in parallel.
> - `[AC<n>]` — helps satisfy acceptance criterion #`<n>` from `proposal.md`.

> **Read [kubelab#1090](https://github.com/mlorentedev/kubelab/issues/1090) first** — it holds the sequencing, the ticket dispositions and the RPO reasoning this list implements. Part 0 is closed; its transcripts are in `verification.md` and several of its answers **contradict what `proposal.md` originally assumed**. Do not implement against the proposal's first draft of R-C or AC4.

## Setup

- [x] Branch created from origin/master: `docs/backup-044-gating-risks` (Part 0 record + this list; implementation branches come later, one per part) ✓ 2026-08-15
- [x] `proposal.md` is complete and acceptance criteria are testable ✓ 2026-08-15 — AC4 corrected on the same date, see Part 0
- [x] No open questions left in `proposal.md` "Risks / open questions" ✓ 2026-08-15 — all three gating risks settled by on-device measurement

## Implementation

### Part 0 — settle the gating risks against live nodes, before any config

Nothing here changed backup state. Each task turned a guess into a recorded fact, and two of them invalidated tasks that would otherwise have been written below. Full transcripts in `verification.md`.

- [x] [P] **R-C — is a boot snapshot orderable before Gitea's first write?** ✓ 2026-08-15 — **yes, but not against the unit the proposal named.** Measured on the Beelink: dockerd starts at +10s, `kubelab-compose.service` at +33s, and Gitea starts at +33.5s — *with the compose unit*, because its `ExecStop` marks containers explicitly stopped and `unless-stopped` honours that. On a power cut `ExecStop` never runs and dockerd resurrects Gitea at +10s, so `Before=kubelab-compose.service` is 23 seconds too late in exactly the scenario an on-demand node exists to survive. **Correct constraint: `Before=docker.service`**, which also makes the snapshot quiescent.
- [x] [P] **R-B — does restic fit in the RPi3's 1GB?** ✓ 2026-08-15 — **yes, with a measured cap.** A 974MiB / 24,668-file backup and a `check` over a 399M repo completed on the 906MB node without an OOM, ~40x the real workload; the streaming fallback is not needed. The cap required fixing a prerequisite first (see below) and then measured out at a floor between 16M and 24M. **Deploy at `MemoryMax=128M` with `MemorySwapMax=0`.**
- [x] **R-B prerequisite — the RPi3 could not enforce any memory limit.** ✓ 2026-08-15 — the memory cgroup controller was absent, so every `MemoryMax` was a silent no-op and two measurement attempts produced nothing. Filed and fixed as **ANSIBLE-040 (#1101, PR #1103)**: new `rpi_boot` role, controller now present, control experiment inverts. Discovered *because* Part 0 measured instead of assuming.
- [x] [P] **R-A — where does the restic repository password live?** ✓ 2026-08-15 — **independent escrow in Bitwarden, decided by the operator.** A password stored only in SOPS is decrypted by an age key a disaster destroys (#479), so recovery would depend on the artefact being recovered from. Bitwarden is hosted and independent of both the age key and the site.
- [x] Record all three answers in `verification.md` with the commands and their output ✓ 2026-08-15 — including the two failed measurement attempts, because the second one nearly shipped a false result.

### Part 1 — the SSOT allow-list, before anything writes a backup

The substrate. #1092's AC3 changes the model from "enumerate volumes minus an exclude list" to "back up exactly what is declared", and Part 0 found the concrete reason it cannot stay volume-shaped.

- [x] [AC6] Add a test that fails when a declared backup source does not exist on disk, **and** when a path exists on a covered node but is absent from the SSOT. ✓ 2026-08-15 — split across `tests/test_backup_sources.py` (static: completeness and the exactly-one-type invariant) and `tests/infra/test_backup_coverage.py` (live: both directions against the node). The second half found a **fourth** orphan on its first run — `portainer_portainer_data` on the VPS, no container at all, frozen since 2026-03-19, in neither #449's catalog nor #452's tiers. Evidence on #1092. Unreachable on-demand nodes skip rather than fail.
- [x] [AC6] Add a `backup.sources` block to `common.yaml`, keyed per node, **enumerating paths rather than Docker volumes**. ✓ 2026-08-21 (#1199) — four nodes declared, and Part 0's evidence held: Gitea is `path: /opt/gitea/data`, not a volume. Both shapes are supported because the RPi3 and RPi4 genuinely are volumes; what matters is that the SSOT names which, per source. Original wording follows — Part 0's decisive evidence: on the Beelink, Gitea is `bind src=/opt/gitea/data` and MinIO is `bind src=/opt/minio/data` — neither is a Docker volume, so `docker volume ls` returns only buildx caches and the runner toolcache. A volume-shaped allow-list would archive rebuildable junk, miss Gitea and MinIO entirely, and report success.
- [x] [AC6] Name the RPi3's Uptime Kuma source as the **volume mountpoint of `uptime_kuma_data`**, and record the discriminator in a comment. ✓ 2026-08-21 (#1199) — `volume: uptime_kuma_data`, and `tests/test_backup_sources.py::test_rpi3_names_the_live_volume_not_the_orphan` pins it against #1092's larger, more canonical-looking orphan. Original wording follows — The orphan `uptime-kuma_uptime_kuma_data` is *larger* (26M vs 22M) and its name looks more canonical; three signals agree on which is live (mtime, size, and `docker inspect uptime-kuma` reporting `volume uptime_kuma_data -> /app/data`). #1092 instance 1 need not be deleted first, but it must not be silently selected.
- [x] [AC1] Cover all four nodes with the `path` and `volume` source types: Beelink (Gitea, a bind mount), RPi3 (Uptime Kuma), RPi4 (Pi-hole `pihole-FTL.db`), VPS (Headscale). Every path was measured on the node, never assumed. ✓ 2026-08-15
- [x] [AC1] Add the `pvc` source type and the VPS's remaining two consumers. ✓ 2026-08-22 (#1236) — with one deliberate difference from this line: **n8n replaced postgres**, because postgres was measured EMPTY (0 tables, 7.5 MB base) while n8n's SQLite carried a 4.1 MB WAL against an 892 KB database. Backing up nothing and skipping the live data would have satisfied the letter of this task and missed its point. postgres joins the day something writes to it, with `pg_dump` rather than `sqlite3 .backup`. Original wording follows — **Deliberately deferred, not forgotten**: restoring a PVC differs in kind — the claim must exist before anything can be written into it — so the design belongs with the restore work in Part 6 rather than with a declaration. Both currently have an in-cluster copy via the PVC CronJob, which is not offsite; that is the gap this closes. Until it lands, the CronJob keeps running and MinIO keeps a consumer, so **this spec does not release #972 even when complete**.
- [x] Leave the existing `roles/backup` untouched in this part. ✓ — honoured, and then Part 3 deleted the role as planned. Its residue in the toolkit CLI outlived it and was retired separately in #1230 (TOOL-038): `toolkit infra backup volumes|list` still read `/opt/backups`, which nothing had written to since, so it reported **fossils** — a directory that simply stops growing, indistinguishable from a healthy backup until someone needs a restore.

### Part 2 — credentials and the destination, before the role that uses them

- [x] [P] Create the Cloudflare R2 bucket and access credentials. R2 needs no provisioning of its own, which is part of why the critical subset goes first (ADR-049 D3). ✓ 2026-08-19 — confirmed live: `make backup-verify-destination` passes bucket-scoped-token, write, read, integrity and delete against `kubelab-backups`.
- [x] [AC1] Register the R2 access key and secret in `SECRET_CATALOG` (`toolkit/features/secrets_manager.py`). **Check `envs` against the ANSIBLE-033 failure mode**: a tuple matching no real env makes the secret vanish from every audit silently, and a backup credential silently absent from the audit is the worst possible thing to have silently absent. Confirm with `make secrets-audit` that the count rises. ✓ 2026-08-19 — both keys registered with `envs=("prod",)`; `make secrets-audit` reports PROD 47/47 (100%).
- [x] [AC1] Escrow the restic repository password in **Bitwarden, not SOPS** (Part 0 / R-A). Document in the runbook exactly which artefact lives where — R2 access credentials in SOPS because they are operational and rotatable, the repository password in Bitwarden because it is the one thing whose loss is unrecoverable and it must not depend on the age key. A split nobody wrote down becomes its own failure mode. Coordinate the vault item with the dotfiles lane (taxonomy changed in dotfiles#982). ✓ 2026-08-19 — operator confirmed the Bitwarden Secure Note exists; `docs/runbooks/offsite-backup-restore.md:22-25` already documents which artefact lives where.
- [x] [P] Decide and record how restic is installed per node. Part 0 measured with a throwaway static binary deliberately, so the install path is an open choice: distro package (absent from the RPi3's apt candidates), pinned static binary, or container. Whichever wins needs a version pin in `common.yaml` like every other image, not a floating download. ✓ 2026-08-19 — **pinned static binary**, the k3s precedent (`get_url` + checksum against upstream `SHA256SUMS`, `restic_version` in `common.yaml`). No container: none of the four backup targets (Headscale, Gitea, Uptime Kuma, Pi-hole) run on Kubernetes, so Helm does not apply — restic is node state, same layer as the systemd timer invoking it. Arch resolved per node at Ansible run time via `ansible_facts.architecture`, the existing `docker`/`dev_node` role idiom — not hardcoded, so a Pi reimaged to a different bitness cannot silently pull the wrong binary.

### Part 3 — the backup role: capture and ship, split deliberately

- [x] [AC2] Write the capture step: `sqlite3 .backup` for all four live SQLite databases (Headscale, Gitea, Uptime Kuma, Pi-hole FTL) into a staging directory, then everything else by path from the Part 1 allow-list. All four were measured with continuously-updating mtimes; a file copy of a live SQLite is not a consistent snapshot. Part 0 demonstrated this by accident — the payload measured 21.6 MiB before the RPi3 reboot and 17.6 MiB after, because the restart checkpointed the WAL. ✓ 2026-08-20 — `roles/node_backup/templates/node-backup-capture.sh.j2`. Volume sources measured live against VPS/RPi3/RPi4 first: neither host nor container has `sqlite3` uniformly (Headscale's container lacks it entirely), so the mechanism is `docker volume inspect --format '{{.Mountpoint}}'` + host `sqlite3` (installed as an unpinned apt package, the same convention `docker-ce` already uses) for every source, `path:` and `volume:` alike.
- [x] [AC7] Write the ship step: restic to R2, one repository per node, with a retention policy and `restic check` inside the scheduled run rather than as a separate audit. ✓ 2026-08-20 — `node-backup-ship.sh.j2`. `restic check` is gated on a `--check` arg (unset by default) so Part 4's weekly timer instance can request it without editing this script; the frequent instance omits it, matching the approved-parameters table's "weekly, not per-run".
- [x] **Keep capture and ship as two units, not one.** Capture must be ordered against `docker.service` (Part 4) and must be fast, because it delays every boot. Ship needs the network and must never sit on the boot path — ordering an R2 upload before dockerd would put a network round-trip in front of Gitea starting on every power-on. ✓ 2026-08-20 — two scripts, two `.service.j2` units; ship declares `After=` capture's unit. `docker.service` ordering is Part 4's (no timer exists yet in Part 3).
- [x] [AC7] Run restic under `MemoryMax=128M` and `MemorySwapMax=0` on the RPi3 (Part 0 / R-B: floor between 16M and 24M, so this is >5x headroom and ~14% of the node's RAM). The swap setting is not decoration — the node has ~900MB of swap and a restic that swaps will thrash the monitoring of record instead of failing cleanly. **This is only enforceable because ANSIBLE-040 landed**; assert the controller is present rather than assuming it. ✓ 2026-08-20 — the ship unit template applies `MemoryMax`/`MemorySwapMax` conditionally; the playbook sets it only for `node_short_name == 'rpi3'`, empty everywhere else, so no other node inherits a cap it was never measured against.
- [x] Replace the VPS's use of `roles/backup` with the new role, and delete the exclude-list enumeration. Its ~306M of dead pre-K3s volumes stop being backed up as a side effect, which is the point. ✓ 2026-08-20 — `roles/backup` deleted; `playbooks/backup.yml` rewritten against `roles/node_backup`, now targeting all four `backup.sources` nodes (beelink, rpi3, rpi4, vps), not VPS alone. **Corrected 2026-08-20 (#1186): as merged it targeted ONE.** The `hosts:` patterns were written as `kubelab-beelink`/`kubelab-rpi3`/`kubelab-rpi4`, but only the VPS carries that prefix in the generated inventory — the others are `beelink`, `rpi3`, `rpi4`. `--list-hosts` on the merged playbook: play 1 matched 1 host, play 2 matched **zero**. Ansible reports an unmatched pattern as a WARNING and exits 0, so the run would have reported success while backing up neither Gitea nor Pi-hole nor Uptime Kuma. This entry claimed four the whole time; the claim is what made it survive review. **Also deleted `playbooks/restore.yml`**, whose only task (`include_tasks: ../roles/backup/tasks/restore.yml`) no longer resolves — it restored from the mechanism this Part replaces. **Known gap, disclosed rather than hidden**: `toolkit backup restore` (`orchestrator.py:69`, no `make` target exposes it today) now fails with a missing-playbook error until Part 6 builds the restic-based restore and its own playbook. Not silently broken — it errors loudly, and nothing today depends on it working.

- [x] **Deploy the role to all four nodes and prove it installed.** ✓ 2026-08-21 (#1199) — `make backup ENV=prod`, fleet powered on: `ok=20 changed=1 unreachable=0 failed=0` on beelink, kubelab-vps, rpi3 and rpi4. Judged on the per-host lines, not the exit code. **The first real deploy reached one node of four**, for a reason no test could have caught: the role unpacked restic's `.bz2` asset with `bunzip2`, and the RPi4 cannot install that package at all — its apt refuses `bzip2 1.0.8-5.1`, which depends on `libbz2-1.0 (= 1.0.8-5.1)`, against the installed `1.0.8-5.1build0.1`. Adding `bzip2` to the package list is not the fix; that IS the package apt refuses, and the cache was already fresh (`cache_updated: false` on a refresh). Decompression moved to Python's stdlib `bz2`: backup machinery has to install on a node that is already degraded, and a broken apt is a degraded node. The RPi4's apt state is **#1198**, deliberately not fixed by hand on the node. **Second defect, from the same failure**: the decompress redirected onto the install path, and a shell evaluates the redirect target *before* it resolves the command — the `rc=127` left a 0-byte `/usr/local/bin/restic` exactly where capture and ship expect a binary. Now a temp path and a move. Destination proven independently: `make backup-verify-destination` (bucket-scoped token, write/read/integrity/delete) and `make backup-verify-restic` (init/backup/snapshots/check) both green.
- [x] **`--check` is not a rehearsal for this role.** ✓ 2026-08-21 — in dry-run the download and the decompression do not happen, so the next task reports `file (/usr/local/bin/restic) is absent, cannot continue` and the host reports failed. Two nodes came out red in check mode and green for real. Useful for reachability and for which hosts a pattern matches; worthless as a prediction of the outcome.

### Part 4 — trigger models, asymmetric on purpose

- [x] [AC1] Always-on nodes (VPS, RPi3): a plain wall-clock `OnCalendar` timer. RPO from the ratified tiers (#452): Tier 1 <6h, Tier 2 <24h. ✓ 2026-08-21 (#1201) — `node_backup_schedule: "*-*-* 0/4:00:00"`, every 4h, with `RandomizedDelaySec` so the small always-on fleet does not hit R2 on the same wall-clock second.
- [x] [AC5] On-demand nodes (Beelink, RPi4): fire **on boot, hourly while up, and on graceful shutdown**. ✓ 2026-08-21 (#1201) — An interval at or above the session length never fires at all, because the homelab runs in 3-4 hour bursts — an RPO trigger is bounded by session length, not by the RPO number (#1090).
- [x] [AC5] Order the boot capture `Before=docker.service`, **not** `Before=kubelab-compose.service` (Part 0 / R-C). ✓ 2026-08-21 (#1201) — rendered only for `location: on-demand`, with R-C's reasoning in the template itself. Do not take the shortcut the proposal originally implied: `Persistent=true` is not an ordering primitive, and the compose unit is not the writer's real start trigger on the path that matters.
- [x] [AC5] Handle the shutdown snapshot's ordering explicitly. ✓ 2026-08-21 (#1201) — `RemainAfterExit=yes` + `ExecStop=`, ordered `After=` the node's own writer-stopping unit (`kubelab-compose.service` on Beelink, `docker.service` on RPi4), so the snapshot is taken while the database is still live. The per-node target is deliberate, not a fleet constant. systemd stops units in reverse dependency order, so a capture ordered `After=` the compose unit stops **first**, while Gitea is still running — a live-database snapshot, which is what `sqlite3 .backup` is for. Decide and document which of the two orderings is wanted rather than inheriting one by accident.
- [x] This asymmetry is deliberate and must not be generalised fleet-wide. Say so in the role's defaults, where the next person changing it will read it. ✓ 2026-08-21 (#1201) — stated in `roles/node_backup/defaults/main.yml` beside the two schedule knobs, with the 3-4h session-length reasoning.

### Part 5 — failure is observable, silence is not

- [x] [AC4] Post a failure envelope to the **NOTIFY-001 fabric** ✓ 2026-08-21 (#1212, #1216) — the mechanism was EXTRACTED rather than copied: `roles/node_notify` installs one `kubelab-notify@.service` template for the whole fleet, and any unit opts in with `OnFailure=kubelab-notify@%n.service`. The four node-path units declare it. `%n` becomes the notifier's instance, verified against a live systemd rather than assumed. Delivery proven end to end on rpi3: `Result=success, ExecMainStatus=0`, and curl uses `-f`, so that is n8n answering 2xx. Original text follows — — `POST https://<n8n>/webhook/notify`, the same envelope `roles/node_maintenance` already posts from bare metal — at severity **`log`** (decided 2026-08-15). **Correction carried from Part 0:** `proposal.md` originally said "the #686 path". No such path exists; `overlays/prod/backup.yaml` has no failure notification at all, only `failedJobsHistoryLimit: 3`, and #686 is the still-open ticket to create routing for minor sources. #686 is related, not blocking.
- [x] [AC4] A window missed because an on-demand node was powered off must produce **no** alert. ✓ 2026-08-21 (#1216) — and it needs NO mechanism, which is the point: a powered-off node produces no run, so no failure, so no envelope. `OnFailure=` cannot alert on absence by construction. Absence is AC9's control, below. Alerting on the homelab being off trains the operator to ignore the channel, which is the same reasoning `common.yaml` already applies to staging probes.
- [~] [AC4] Demonstrate both halves by **injecting a failure** (bad credential, unreachable bucket) and by a **real power cycle** — not by configuration review. **First half ✓ 2026-08-22**: both injections run on beelink — blackholed endpoint and invalid credential — each producing a genuine unit failure and a delivered envelope, plus a synthetic-unit control. Evidence in `verification.md`. **Second half still open**: the power cycle needs the operator. Finding, **found and fixed the same day**: every failure surfaced as `Result=timeout` after ~180s with no restic error at all. Two causes — restic's 5m `--stuck-request-timeout` outliving the unit, and the repository probe discarding stderr. Re-injecting the same bad credential now names it: *"The request signature we calculated does not match... Check your secret access key"*.
- [x] [AC9] **A run that never happened is reported.** ✓ 2026-08-22 (#1221) — a push heartbeat as ship's LAST step, watched by Uptime Kuma on the RPi3, outside the failure domain it watches. Window 6h = #452's ratified Tier 1 RPO, not a margin over the 4h schedule. **Muting is entirely tag-driven** (`not (tags & muted_notification_tags)`); the seed's own `notificationIDList` is ignored by the apply path, so an `on-demand` tag is what keeps homelab nodes visible-but-silent. Observed live 2026-08-22: rpi3 and vps at `beats=1`, beelink and rpi4 at `0` because the homelab is off — the design working, not a gap. **The demonstration this line asks for (stop a timer, observe the alert) is NOT done** — see the open item below. One place answers "when did each node last succeed", and a node past its window alerts unprompted. This is the coverage view, and it is the failure that survives every check asserting only that the snapshots which do exist look healthy. Demonstrate it by stopping a timer, not by reading the query.
- [x] Reuse `node_maintenance`'s hardening rather than re-deriving it: bounded curl timeouts, UTF-8-safe truncation, and the token passed so it is not visible in `ps` (ANSIBLE-038, #1088). ✓ 2026-08-21 (#1212) — all five moved verbatim WITH their measurements, which is why extraction beat copying: a second copy is a copy that drifts, and the drift costs a notification that never arrives. The heartbeat call takes the same treatment — Kuma wants the token in the URL *path*, so it rides curl's config on stdin rather than argv.

- [x] [AC4/AC9] **The live demonstrations.** ✓ 2026-08-23 — **all four now done.** The fourth, and the one that needed an operator willing to leave a node unbacked for six hours: the VPS ship timer was stopped at 02:14:54Z against a last heartbeat of 00:01:04Z, and `[Ops · Backup · vps node-path] [Down] No heartbeat in the time window` reached Slack at **06:01:17Z** — against a prediction of ~06:01:15Z, recorded before the wait began. Unprompted, with the monitor untouched between arming and firing; shortening its interval to force the result was considered and rejected as no longer answering the criterion. The negative control arrived free in the same window: the operator powers the homelab off by **smart plug**, and across a full off/on cycle the VPN monitors for beelink and jetson paged while **no** `Ops · Backup · <homelab node>` alert did — the `on-demand` tag muting, observed rather than designed. Transcript in `verification.md`. Original text follows — Three of four done 2026-08-22 with the homelab powered on: the failure injection (two independent causes on beelink — blackholed endpoint and invalid credential — each producing a real unit failure and a delivered envelope, #1251), the **power cycle showing no alert** (beelink off, zero notifications — the `on-demand` tag's muting verified rather than designed), and the boot-ordering half of AC5 (`capture complete` 07:05:07, `Starting docker.service` 07:05:11). **Still open: stopping a timer and watching the coverage monitor page.** That one needs an operator willing to leave a node deliberately unbacked for six hours. Original wording follows — Both halves are built and neither is demonstrated the way these criteria demand — by causing the failure, not by reading the config:
  - **Inject a failure** (bad credential, unreachable bucket) and observe the envelope arrive. The delivery path is proven; a *genuine* backup failure raising it is not.
  - **Stop a timer** and observe the coverage monitor alert unprompted, plus **a real power cycle** on an on-demand node showing no alert. The heartbeat is observed arriving (rpi3, vps) and correctly absent (beelink, rpi4, homelab off), but nobody has watched a monitor go DOWN and page.
- [x] **A way to run one backup now, rather than waiting for a timer.** ✓ 2026-08-22 (#1224) — not in the original plan, and the gap was structural rather than cosmetic: the PVC class has had `make backup-pvc` since ADR-024 and the node-path class had nothing, so an operator about to do something risky could not take a snapshot first. `make backup-node NODE=x ENV=prod` starts the ship unit, which pulls capture in via `Wants=` — deliberately the same path the timer takes, so it also posts the heartbeat. It is what made AC9 observable end to end on the day it merged instead of at the next tick.

### Part 6 — a backup that has never been restored is a hypothesis

- [x] [AC3] Restore Gitea end-to-end into an **ephemeral instance**, never over the live `/opt/gitea/data`. ✓ 2026-08-22 — restored from R2 to a scratch dir, booted `gitea/gitea:1.25.5` against it, and the SERVICE answered:

  ```
  /api/v1/version  -> HTTP 200  {"version":"1.25.5"}
  /api/v1/users/manu -> login=manu id=1 created=2026-08-14
  /user/login      -> <title>Sign In - Gitea…</title>
  ```

  Gitea's own `doctor` passed first (`Check Database Version: OK`, `Check consistency of database: OK`) — the binary accepting the data, not a test reading it. Live `/opt/gitea/data` untouched, verified afterwards; container and scratch removed. Original wording follows — Restoring over the thing being protected to prove the backup works risks exactly what the backup exists for. Doubles as the #492 DR drill; do not expand scope to close #492.
- [x] [AC2] Restore one SQLite database and run an integrity check **on the restored copy** — content verified, not exit codes. ✓ 2026-08-22 — TWO databases, restored from R2 into a scratch directory on the workstation, never over the live data:

  ```
  n8n/database.sqlite     integrity=ok   63 tables   credentials_entity: 1 row
  authelia/db.sqlite3     integrity=ok   26 tables
  ```

  **The number that proves the mechanism**: `database.sqlite` is 892 KB on disk and **946 KB inside the snapshot**. `sqlite3 .backup` folded in the 4.1 MB WAL; a file copy would have stored the smaller, older file and restored cleanly as a database missing its recent history. Measured, not argued.
- [x] [AC3] Restore at least one always-on consumer into a scratch location, with the transcript in `verification.md`. ✓ 2026-08-22 — the VPS: Authelia and n8n restored from R2 and integrity-checked on the restored copy (`integrity=ok`, 63 and 26 tables). Transcript in `verification.md`.
- [x] [AC1] Verify every **node-path** consumer in the proposal's table is present, by listing snapshots **from a machine that is not the source node**. ✓ 2026-08-22 (#PR) — `make backup-coverage ENV=prod`, run from the workstation with the homelab powered off: all four covered, newest snapshot and its age per node. The criterion had no tool until now; this is it, and it exits non-zero when a declared node has no repository or no snapshot. **The one thing that cannot be inferred is the repository NAME**: `node_backup_r2_repository` is built from `inventory_hostname`, and `networking.vps.hostname` is already `kubelab-vps` while homelab nodes are bare — a first revision inferred the prefix from the node's position and produced `kubelab-kubelab-vps`, reporting the VPS as UNCOVERED with a nightly backup in R2. Read from the SSOT now, pinned in `tests/test_backup_coverage_report.py`. Reading the playbook is not verification. The PVC class is out of scope by mechanism — BACKUP-046 (#1111), not a gap in this criterion.
- [x] [AC8] ✓ 2026-08-22 — grepped `roles/node_backup/`, `common.yaml`'s backup block and `backup_destination.py`: the only occurrence of `minio:9000` anywhere in the pipeline is a COMMENT explaining what the old CronJob targets. Zero code paths. The four files that do reference it are MinIO's own manifests and the CronJob being retired. Original wording follows — Confirm this spec introduced no new consumer of `minio:9000`, the guarantee #1056's original AC2 was reaching for. Note that closing this does **not** release #972 on its own — other PVCs still target MinIO, and #1056's "blocked by #972" was backwards.

## Approved operating parameters

Decided by the operator 2026-08-16 on the measured arithmetic. Bake these in; do
not re-open them during implementation.

| Parameter | Value | Why |
|---|---|---|
| Always-on schedule | every **4h** (wall-clock) | #452 ratifies Tier 1 at RPO < 6h; hourly over-delivered and cost 119k operations a month to do it |
| On-demand schedule | hourly **while up**, plus boot and graceful shutdown | a 6h threshold never fires in a 3-4h session (#1090) |
| `restic check` | **weekly**, not per-run | the remaining operations lever; leaves 16x headroom for repository growth |
| Retention | `--keep-daily 7 --keep-weekly 4 --keep-monthly 6` | ~a year of restore points; dedup makes the history nearly free (42 snapshots cost 2.6 MB in the rig) |
| Storage class | **Standard** | the free tier does not apply to Infrequent Access — the cheaper-looking class is the more expensive one here |

Projected: ~63,000 Class A operations a month against 1,000,000 free, and ~32 MB
of source against 10 GB.

## Follow-ups, not this spec

- [ ] Unblock #1076 (repos into Gitea) once AC3 is green — #1090's step [4] gates it on an exercised restore, and populating the forge first makes the first repository the first thing with no recovery path.
- [ ] #1092's orphan cleanup stays separate; this spec only has to name the live source unambiguously.
- [ ] The bulk tier (Hetzner Storage Box + Borg, #471/#473) is a different pipeline with its own provisioning.
- [ ] The **PVC consumer class** is BACKUP-046 (#1111) — Authelia, Postgres, Grafana,
  n8n, CrowdSec db. Three of those five have no backup at all today, and Postgres has
  no mechanism in any spec: it needs `pg_dump`, not `sqlite3 .backup`. Do not let it
  creep in here. **The split is by mechanism, and the tier axis cannot make it**,
  because Authelia and Postgres are Tier 1 *and* PVCs.
