---
tags: [spec, tasks]
created: "2026-08-14"
---

# Tasks - AUTH-004-identity-and-machine-access

> TDD order. One task = one focused commit. Tick as you go. Reorder freely while spec is in `draft` state; freeze once you start `implementing`.
>
> **Inline markers:**
> - `[P]` — no dependency on another unchecked task, safe to run in parallel.
> - `[AC<n>]` — helps satisfy acceptance criterion #`<n>` from `proposal.md`.

> **Read [ADR-062](../../docs/adr/adr-062-platform-identity-model.md) first.** This list implements it. The single-identity plan recorded on #1013 was reversed on 2026-08-14; anything written against "rename `manu` to `operator`" is stale, including the ordering constraint it implied.

## Setup

- [x] Branch created from origin/master: `docs/adr-platform-identity-model` (ADR + amended proposal + this list; implementation branches come later, one per part) ✓ 2026-08-14
- [x] `proposal.md` is complete and acceptance criteria are testable ✓ 2026-08-14 — amended to the two-tier model, ACs rewritten (AC2 tiers, AC7 break-glass drill added)
- [ ] No open questions left in `proposal.md` "Risks / open questions" — four remained (R1, R2, R5, R6) and every one of them is settled by observing a live instance, never by reading documentation. Part 0 exists to close them. Do not write configuration against a guess. **2026-08-15: R2, R5 and R6 are settled with transcripts in `verification.md`. R1 is still open and stays open until the operator authorises it — it touches prod Authelia.**

## Implementation

### Part 0 — settle the open questions against live instances, before any config

Nothing here changes state. Each task turns a guess into a recorded fact, and every one of them can invalidate a task below it — which is why they come first rather than being discovered mid-implementation.

- [x] [P] **R2 — inventory what is attached to Gitea's existing `manu` user id.** OIDC linkage, registered SSH keys, existing access tokens, and anything owned. Record the output in `verification.md`. **This is the task that decides whether the two-tier model's central claim holds** — that `manu` survives in place, so no account is deleted and no ownership migrates. If something forces a recreate, the pre-first-push deadline from the original plan comes back and Part 1 must run before the operator's first push. Do this one first, not in parallel. ✓ 2026-08-15 — **the claim holds.** uid 1 = `manu`, admin, and its only attachment is one SSH key (`msi-workstation`); zero repos, zero tokens, zero org membership, so no recreate is forced and the deadline stays dissolved. Three things surfaced that the task did not anticipate: (a) the live admin is `manu` while `common.yaml:469` declares `operator` — #951's drift, measured; (b) its root cause on this delivery path is `provision-bee.yml:222`, `gitea_admin_user: "{{ gitea_secrets.basic_auth.user }}"`, the *same* basic-auth alias as `k8s_secrets.py:52`; (c) `manu` has **no OIDC linkage at all** (`login_type: 0`, `external_login_user` empty), so ADR-062's superadmin presupposes a linking step nobody has taken — which sharpens R1 into "does the first SSO login link to uid 1 or fork a second account".
- [x] [P] **R5 — does Gitea derive admin from a group claim?** Read the live binary's own help (`gitea admin auth update-oauth --help`), not the documentation, for an admin-group option. Decides whether `manu`'s superadmin bit is declarative (a claim mapping, reconcilable) or a provisioning step (an explicit `--admin` on the account). `toolkit/scripts/configure_oidc.py` already drives `update-oauth`, so whichever answer comes back has a home. ✓ 2026-08-15 — **declarative.** Gitea 1.25.5 exposes `--group-claim-name`, `--admin-group`, `--restricted-group` and `--group-team-map`; `--restricted-group` is a ready-made lever for the `operator` tier that ADR-062 D2 did not know existed. None of them is currently passed: the bootstrap requests the `groups` scope and then ignores the claim. **Correction to this task's own premise** — the answer's home is *not* `configure_oidc.py`, which still `kubectl exec`s into a Gitea pod that ADR-061 deleted; it is `roles/beelink_services/files/gitea-bootstrap.sh`.
- [x] [P] **R6 — what does MinIO do with the `groups` claim?** ADR-016 asserts it maps groups to policies; nothing in this repo configures that mapping. Establish the live behaviour before claiming a tier is enforced there. ✓ 2026-08-15 — **nothing.** The running container has no `MINIO_IDENTITY_OPENID_*` variable at all: root credentials only, no identity provider, so no claim reaches it. ADR-016's assertion is aspirational. AC2 therefore cannot be demonstrated on MinIO — recorded as a named gap per ADR-062 D5, not counted as passing.
- [x] [P] **R4 — how is login prohibited on a bot account?** ✓ 2026-08-23 — **not through the CLI; through `PATCH /api/v1/admin/users/{username}` with `prohibit_login: true`.** This task did not exist until today: R4 is named as a risk in `proposal.md` and Part 3 is written as "gated on R4, R5", but Part 0 never listed it, so nothing was going to settle it. `gitea admin user` has six subcommands (`create`, `list`, `change-password`, `delete`, `generate-access-token`, `must-change-password`) and none touches `prohibit_login`; `--restricted` is a visibility tier, not a login block, and must not be read as a substitute. The admin API does expose it, and the asymmetry decides the provisioning ORDER: `prohibit_login` is in `EditUserOption` and **not** in `CreateUserOption`, so it cannot be set at creation and the bot exists briefly in a loginable state — create, then PATCH with a superadmin token, then mint the scoped token. AC5 survives as "provisionable from Ansible", but Part 3's line below understates it: it needs a superadmin token to exist first. ADR-062 D1's other candidate (never declaring the bot in Authelia) stays available but is omission, not enforcement; the spec asked for enforced.
- [ ] [P] **R1 — which flag combination enables OIDC-only onboarding.** Belongs to Part 2 but is cheap to settle in the same pass: create a throwaway Authelia user, attempt an SSO login against Gitea, observe. Both candidate mechanisms are in `proposal.md`; the answer is which one actually admits a new user while self-service registration stays closed. **STILL OPEN, deliberately** — it touches **prod** Authelia, so it needs the operator's explicit go-ahead before running. R2 also raised its stakes: it must now also answer whether an SSO login as `manu` links to the existing uid 1 or forks a second account. **2026-08-23 — the go-ahead was given for the reversible half and R1a is settled in `verification.md`: with today's configuration the SSO flow completes and then parks at `/user/link_account`, so onboarding needs a human at a form and AC3 fails by measurement, not by inference. Prod needed no change — the e2e `testuser` already existed in prod Authelia, so nothing was created and nothing reverted. Two things remain, and neither is this task: the headline "which flag combination admits a new user" cannot be answered without changing a flag on the live instance (a separate authorisation, and the change belongs in `gitea-bootstrap.sh` per R5), and **R1b stays parked**.**
- [x] Record all four answers in `verification.md` under a `## Open questions, settled` heading, each with the command run and its output. A settled question with no transcript is an assertion. ✓ 2026-08-15 — three of four recorded with full transcripts; R1's slot is left explicitly open rather than filled with a prediction.

### Part 1 — identity resolves from a declared SSOT map

The core fix, and it stands alone: no live account is touched, so it is safe to ship before Parts 2 and 3 and before the open questions above are fully settled — with the one exception that R2 may add a migration task to it.

- [x] [AC6] Add a test that fails when a service's admin identity resolves from anything other than `apps.auth.identities`. Assert on the generated Secret content, not on the source file, so the test survives a refactor of how the value is plumbed. **Demonstrate it red first** — it must fail against today's `BASIC_AUTH_USER` mapping. ✓ 2026-08-24 (#1390) — `tests/test_admin_identity_ssot.py`, 8 tests, asserting on generated Secret content as required.
- [x] [AC1] Add `apps.auth.identities: {superadmin: manu, operator: operator}` to `common.yaml`. Keep `apps.auth.admin_username` present for one commit so nothing breaks mid-change; its removal is a later task in this part. ✓ 2026-08-23 — **landed early and out of order, under incident pressure, and that deviation is recorded here rather than left to be inferred.** Part 1 was planned as one sequence starting from a red AC6 test; instead only the **Gitea leg** shipped, because a prod credential rotation rotated `basic_auth.user` and the alias at `provision-bee.yml:227` turned that into a change of Gitea's admin identity — taking SSO down and breaking the repair path with it. The remaining AC1 tasks (Grafana, MinIO, both generators, `admin_username` removal) are untouched and still owe the red-test-first discipline; that test asserts on generated K8s Secrets, which is not the Ansible path fixed here, so the discipline survives the split intact.
- [x] [AC1] Point `grafana-admin`'s `admin-user` at the resolved superadmin instead of `BASIC_AUTH_USER` (`toolkit/features/k8s_secrets.py:52`). The SSOT value is plaintext config rather than a SOPS-sourced env var, so this likely belongs in `SecretMapping.literals` rather than `keys` — check which before writing. ✓ 2026-08-24 (#1390) — **literals**, as suspected: `admin-user` is gone from `SecretMapping.keys` and emitted by `_build_dynamic_literals` from `_resolve_superadmin(cm)`.
- [x] [AC1] Do the same for MinIO's root user and for Gitea's admin user. Note the two live in different places: MinIO's is a SOPS key consumed by `minio-secrets`, Gitea's is an Ansible variable rendered into the Beelink compose file by `roles/beelink_services`. One decision, two delivery paths — neither should keep resolving from the basic-auth alias. ✓ 2026-08-26 (#1451) — **closed, and this task's own premise was wrong throughout; the sub-notes below are kept rather than rewritten because the wrong premise is the finding.**
  - Gitea ✓ 2026-08-23/24 — `provision-bee.yml:245` renders `gitea_admin_user` from `gitea_config.apps.auth.identities.superadmin`.
  - MinIO ✓ on the **K8s** leg only — `_build_dynamic_literals` emits `minio-secrets.MINIO_ROOT_USER` from the superadmin.
  - MinIO ✗ on the leg that actually runs. The premise said MinIO's path is a SOPS key consumed by `minio-secrets`; MinIO does **not** run in the cluster for this purpose — it runs on the Beelink from Docker Compose, and `provision-bee.yml:261` still reads `minio_root_user` from `secrets.apps.services.data.minio.root_user`, which `credentials.py:497` seeds from the same `common_username` that feeds `basic_auth.user`. So MinIO has Gitea's shape, not the one written here, and the alias this ticket exists to remove is still live on it.
  - Measured 2026-08-26 on the running container: `MINIO_ROOT_USER=manu`. The image also sets `MINIO_ROOT_USER_FILE=access_key`, so the env var is only the effective source if no such file exists — `docker exec minio ls /run/secrets/` → `No such file or directory`, so it is. **It currently equals `identities.superadmin` by coincidence, not by resolution**, which is why nothing looks broken and why the next `credentials generate` can rename it silently (lesson-379's shape). Related but distinct from #1355 facet 3, which is the credential *value* diverging rather than the resolution *path*.
  - MinIO ✓ 2026-08-26 (#1451) on the leg that runs. `provision-bee.yml` resolves `minio_root_user` from `config.apps.auth.identities.superadmin`, and the alias was **disarmed rather than bypassed**: `credentials generate` no longer seeds the SOPS key and its `SECRET_CATALOG` entry is gone, so what it rewrites drops 25 → 24 (corrected in three docstrings and `CLAUDE.md`). Guarded by `tests/test_ansible_identity_ssot.py`, the Ansible-path sibling of the K8s guard — which is why this leg survived two days after Gitea's: **one decision, two delivery paths, and only one of them had a test.** A stale value for the retired key may remain in the `.enc.yaml` files; nothing reads it and `secrets-audit` does not see it.
- [x] [AC1] Replace the `is_admin: true` indirection with an identity-key reference in the Authelia users list, in **both** generators that resolve it (`toolkit/features/generator_authelia.py:171-175` and `toolkit/features/k8s_secrets.py:248-260`). Two generators reading the same SSOT by two mechanisms is the drift SSOT-014b was written to stop; this is the chance to leave one. ✓ 2026-08-24 (#1390) — both resolve through `configuration.resolve_user_identity`; `is_admin` survives only in comments explaining what it replaced.
- [x] [AC1] Add the second human's argon2 hash key to SOPS and register it in `SECRET_CATALOG` (`toolkit/features/secrets_manager.py:122-130`, whose comment already anticipates this rename). **Check `envs` against the ANSIBLE-033 failure mode**: a tuple that matches no real env makes the secret vanish from every audit silently. ✓ 2026-09-02 (#1514, merged 01:43Z) — `users_manu_password_hash` is registered at `secrets_manager.py:231` and written to prod SOPS. The ANSIBLE-033 check was made rather than assumed: `envs=("prod",)`, narrow **on purpose**, with the reasoning inline — declaring all three envs would make dev and staging report a gap for a key never written there, which is the noise that trains an operator to stop reading the audit. Confirmed by name, never by value: `make secrets-audit` → **prod 76/78, staging 56/56, no identity gap**; the key is absent from prod's Missing list. The two prod gaps are `gitea.admin_token` (TOOL-035's blocker, an operator minting step) and `grafana.alerts_ro_token` (OBS-019/#1395) — neither is this spec's. **The caveat stands rather than being rounded off**: the hash has NO consumer today. `manu` is still not a declared Authelia user (`apps.services.security.authelia.users` holds `identity: operator` and `testuser`), so this is Part 1 preparation, deliberate rather than forgotten. Declaring the user is coupled to R1b, which stays parked: `manu` has no OIDC linkage, so its first SSO login is one-shot on the sole admin account.
- [x] [AC1] Remove `apps.auth.admin_username` and confirm nothing still reads it. `credentials.py:298-301` reads it as a default — that consumer needs the map too. ✓ 2026-08-24 (#1390) — gone from `common.yaml`; every remaining occurrence in `toolkit/` and `infra/config/values/` is a comment naming what it replaced.
- [x] [AC1] Confirm the red test from the first task now passes, and that `make secrets-audit` reports no gap for either environment. ✓ 2026-08-26 — `tests/test_admin_identity_ssot.py` green, and `make secrets-audit`: **staging 35/35, prod 53/54**. Ticked with the discrepancy stated rather than rounded: the single prod gap is `apps.services.observability.grafana.alerts_ro_token`, which belongs to OBS-019 (#1395) and predates this spec. **No identity gap in either environment**, which is the criterion. Dev shows 27/31; those four are the stale Gitea/OIDC catalog entries left by ADR-061, also not this spec's.
- [x] [AC1] Deploy to staging and authenticate against each of the three services as the resolved superadmin — a live response per service, per the acceptance criterion. A config diff is not evidence. ✓ 2026-09-01 — **3/3, and no deploy was needed: staging was already carrying the resolved identity**, so the task's "deploy and" half was a no-op verified rather than skipped (`grafana-admin.admin-user` read from the live Secret = `manu`). The 2026-08-24 transcript above had closed Grafana, MinIO and Authelia; **Gitea was the missing third**, absent for a correct reason — its leg did not ship until #1451 on 2026-08-26. Gitea's is also the leg whose delivery path differs (an Ansible variable in the Beelink compose file, not a generated K8s Secret), so it was the one still undemonstrated. Run with controls per the bar the earlier section sets: right-user/wrong-password and wrong-user/right-password both `401`, real `200` with `login='manu' is_admin=True`. **lesson-382 recurred and the control caught it, not the lesson** — the first probe read Authelia's `302` as Grafana's verdict and MinIO's S3 `BadRequest` as a rejected credential; both are answers from something in front of the service. Transcript in `verification.md`.

### Part 2 — SSO onboarding (gated on R1)

- [x] [AC3] [AC4] Configure Gitea per R1's settled answer, declaratively rather than by hand — the CLI-vs-web-process caching gotcha in CLAUDE.md applies, so a restart is part of the change, not an afterthought. **Route corrected by R5:** this goes in `roles/beelink_services/files/gitea-bootstrap.sh` (which already drives `update-oauth` idempotently over `docker exec`), **not** `toolkit/scripts/configure_oidc.py` — that script still `kubectl exec`s into a Gitea pod that ADR-061 removed. Adding `--group-claim-name groups --admin-group admins` (and `--restricted-group` for the `operator` tier) to the existing call is the whole change. ✓ 2026-09-24 — #1809, provisioned in prod: `changed=7`, then `changed=0`. See `verification.md` 2026-09-24.
  - **Amended 2026-09-23. R1 and R5 settled from the Gitea 1.25.5 source, to be confirmed by the logins below.** R1: `oauth2_client.ENABLE_AUTO_REGISTRATION=true`, because the auto-registration branch of `SignInOAuthCallback` checks only `ALLOW_ONLY_INTERNAL_REGISTRATION` and ignores `DISABLE_REGISTRATION`. The sign-up form stays closed (AC4), so the second candidate, which reopens registration, is rejected. It is completed by `USERNAME=preferred_username` and `ACCOUNT_LINKING=auto`. `createUserInContext` links a colliding login to the existing account, by username first and then by email; that is how `manu`'s local account gains SSO without being recreated. Pre-binding `login_name` cannot work, because an unlinked login is matched on `sub`. R5: `update-oauth` and `add-oauth` both take `--group-claim-name groups --admin-group admins --required-claim-name groups --required-claim-value users`, and the admin flag is re-synced from the claim on every SSO login, with one exception: Gitea skips a synced demotion of the last admin (`services/user/update.go`), so a mistake in the group mapping fails safe for the superadmin while it is the only admin. The values are part of the bootstrap's recorded state, so changing them restarts Gitea. **`--restricted-group` is dropped.** The operator tier is enforced by leaving `admins` (Part 2, AC2), not by restricted mode, which also hides every repository the account is not explicitly granted. **New invariant found while designing it:** every Authelia user needs a distinct email. With `auto` linking, a shared email links one identity's login to another's account, and Grafana, which keeps email unique across users, fails the second login. `manu` declares `manu@mlorente.dev`, the address its Gitea account already holds. The Gitea admin is now created with the superadmin's email rather than `apps.contact.email`, which is `operator`'s: on a rebuilt forge, the old derivation would have linked `operator`'s first login to the admin account. Guarded in `tests/test_admin_identity_ssot.py`, `tests/test_ansible_identity_ssot.py` and `tests/test_gitea_bootstrap_idempotence.py`, five mutants killed.
- [x] [AC3] Declare a throwaway user in `apps.services.security.authelia.users`, deploy, log into Gitea through SSO, and confirm an account appears with no manual step in Gitea. Capture the transcript, then remove the user. ✓ 2026-09-24 — (a) to (d) all demonstrated in prod; see `verification.md` 2026-09-24.
  - **Amended 2026-09-23: the real identities replace the throwaway user**, because between them they cover every path. After `make provision NODE=bee ENV=prod`: (a) `manu`'s first SSO login links to the existing local admin: one account, not two, and the break-glass basic-auth login still returns 200 afterwards (linking writes only `external_login_user`). (b) `operator`'s first SSO login creates an account with no step in Gitea. It is admin until Part 2's AC2 task takes `operator` out of `admins`. (c) `testuser`, now in group `e2e`, is refused by the required claim. (d) Re-run the AC4 sign-up probe: `ENABLE_AUTO_REGISTRATION` touches the path it tested, so the tick below is re-demonstrated, not carried forward.
- [x] [AC4] Attempt an unauthenticated registration against Gitea and confirm it is refused. Demonstrated, not read off `app.ini`. ✓ 2026-08-23 — **refused at the handler with `403`, and so is the post-SSO registration path**, which the task did not think to ask about. `r1_signup_probe.py` + transcript in `verification.md`. Two cautions this produced: the `link_account_signup` form is an **empty shell** when registration is disabled (CSRF token + a refusal sentence), so posting it as rendered measures the template and returns a meaningless `200` — the fields must be injected to put the handler under test; and **this tick is conditional on `DISABLE_REGISTRATION` staying `true`**. AC3 and AC4 pull on that same flag in opposite directions, so if Part 2 adopts R1's second candidate this criterion must be re-demonstrated, not carried forward. **Re-demonstrated 2026-09-24**, after `ENABLE_AUTO_REGISTRATION`: anonymous `POST /user/sign_up` → 403, `AC4 ... HOLDS`.
- [ ] [AC2] Log in as `operator` and confirm it is **refused** an administrative action that `manu` can perform. Where the service does not consume the group claim, record it as a named gap with its issue (Grafana → #951) rather than passing over it — per ADR-062 D5, an unenforced tier is the failure this model exists to prevent.
  - **Amended 2026-09-24, in two steps.** Measuring came first, and it found the tier unenforced. `make auth-review` (new) reads each app's live privilege over the break-glass path. Its first prod run showed `operator` as Viewer in Grafana despite `admins`, and Argo CD showed no groups at all (User Info): Authelia 4.39 sends `groups` only in UserInfo (lesson-457). Step 1 (this PR) makes the tier enforced: Grafana's role path defers to UserInfo, Argo CD sets `enableUserInfoGroups`, and `auth review --apply` reconciles. Step 2 takes `operator` out of `admins`, runs `make auth-review ENV=prod APPLY=1`, and then demonstrates the refusal **from `operator`'s sessions that were already open**: Gitea and Grafana read the tier per request, and Argo CD within its 5-minute UserInfo cache.
  - **Amended 2026-09-24: step 1 left Grafana unenforced. Grafana's door is fixed first, which pulls the single-door item for Grafana forward.** After step 1 deployed, Argo CD measured `BOUNDED` and showed `admins` in User Info. But `operator`, logging in to Grafana staging through the route, stayed Viewer: "Synced via Auth Proxy". Grafana's auth proxy logged users in from Authelia's `Remote-User` header before OAuth ever ran, and the proxy maps no role (lesson-458), so step 1's role path was never reached. The fix: `GF_AUTH_PROXY_ENABLED=false` and `GF_AUTH_GENERIC_OAUTH_AUTO_LOGIN=true`. The ForwardAuth stays as a perimeter. Break-glass opens `/login?disableAutoLogin=true` (a new optional `path` in the declaration). All six accounts carried only the proxy label, and Grafana's OAuth never matches an existing account by login (lesson-459). So `GF_AUTH_OAUTH_ALLOW_INSECURE_EMAIL_LOOKUP=true` goes in as a **migration flag**. It adopts each account by its distinct email on that account's first OAuth login, and **comes out in a follow-up PR** once every account in both environments shows the OAuth label. Evidence: in staging, `operator` logs in through the route and `make auth-review ENV=staging` reads `operator live=Admin OK` **without `APPLY`**, then `testuser` logs in and gets Viewer. That is the tier enforced at login, which #951 asks for.

### Part 3 — machine identity (gated on R4, R5)

- [x] [AC5] Provision a bot account from Ansible: account created, login prohibited per R4's settled mechanism, scoped token minted by `gitea admin user generate-access-token`, token written to SOPS and registered in `SECRET_CATALOG`. ✓ 2026-08-26 — **done except "login prohibited", which R4's settled mechanism cannot deliver.** `hefesto` (`apps.auth.identities.machine`) is created, the token is minted with `write:repository,write:user` and recorded in SOPS through `--stdin`, and `SECRET_CATALOG` registers it as `EXTERNAL` with `envs=("prod",)`. Two full provisions report `changed=0`. **But `prohibit_login: true` disables API token authentication as well as the login form** — measured in both directions on a clean account, twice — so a blocked account and a working token are mutually exclusive on Gitea 1.25.5. Binding to the Authelia source keeps the token alive but Gitea still accepts a local password there, which is omission rather than enforcement. Recorded as a **named gap per ADR-062 D5** in the script's own comments and in `verification.md`; what stands in place of the flag is a random creation password nobody holds, absence from Authelia, and no administrative scope.
- [x] [AC5] Prove both halves of the scope: the token performs an operation its scopes allow, **and is refused** one they do not. Capture both responses. A token that works proves only half the claim. ✓ 2026-08-26 — allowed: `GET /user` 200, `POST /user/repos` 201, `GET /repos/hefesto/...` 200, cleanup `DELETE` 204. Refused: `GET /admin/users` 403, `POST /admin/users` 403. **An earlier run of this same check was void and is recorded as such**: every call returned 403 because the account was in a rejected state, so the refusal half proved nothing — an account-level rejection and a scope-level one are the same status code.
- [ ] [AC5] Confirm the bot cannot authenticate through Authelia. **Not demonstrated.** `hefesto` is absent from `apps.services.security.authelia.users`, so no credential exists for it in the IdP — but absence is not a demonstration, and this criterion asks for one. Needs an attempted first-factor as `hefesto` against prod Authelia, expecting refusal.

### Part 4 — break-glass and prod

- [x] [AC7] Declare the break-glass path of every Authelia-dependent service once, with the set derived rather than listed, and one command to open it. ✓ 2026-09-23. `apps.services.security.authelia.break_glass` + `toolkit auth break-glass` / `make break-glass`. `tests/test_break_glass.py` was red first on the real repo: 6 dependents in staging, 8 in prod, exactly the manual inventory. Verified live with `--dry-run` for all four forms, and end to end for Gitea: the private EndpointSlice URL plus `manu`'s local password returns `admin: true`, bypassing Traefik and Authelia. Grafana's local admin answers 200 through a port-forward in both envs, with a 401 control. **Design change, recorded here:** break-glass goes over the private network, never the public route. Grafana's route carries the `authelia` ForwardAuth middleware, which strips `Authorization` and would block `/login?disableAutoLogin=true` with the IdP down, so a public-route path could never have worked there.
- [x] [AC7] Rotation over the declaration. ✓ 2026-09-23. `toolkit secrets rotate --group break-glass --env <env>`: the SOPS value must open the service (else drift, nothing touched), then write-ahead to SOPS, then the service's own API over the break-glass private path, then the new value must log in and the old must not. Any failure restores SOPS, and the service too. Measured before building it:
  - `GF_SECURITY_ADMIN_PASSWORD` only seeds a fresh install, and `gitea-bootstrap.sh` sets the admin password only on create, so both catalog `rotate_note`s were false. They are corrected.
  - Gitea 1.25.5's `EditUserOption.login_name` is `binding:"Required"`, so it is always sent.
  - The single-key `rotate` now refuses these keys, and `credentials generate` preserves them instead of resetting them to its shared prompt password (#1355).
  - Live in staging: Grafana rotated, the new value logs in and the old one does not. An independent port-forward probe gives 200 with the new SOPS value and 401 for the control. `make apply-secrets ENV=staging` refreshed the seed Secret.
  - Live in prod, 2026-09-23 (#1801): the operator ran it, and both Gitea and Grafana reported the new value logging in and the old one refused. This is the first live Gitea run: in staging Gitea does not depend on Authelia, so it has no break-glass account to rotate. Re-verified afterwards from the committed file, over each private path: `manu` logs in on both. `make apply-secrets ENV=prod` then reported all 12 Secrets `unchanged` on a second run.
- [ ] [AC7] Grafana's break-glass becomes a separate local account (#951, option A; ADR-062 D4 amendment). Once OIDC is Grafana's only login (#1825), `manu`'s SSO login would link the break-glass row, and Grafana refuses every password change on an SSO-linked account (`errOnExternalUser`). The declaration becomes `{login: breakglass, email: breakglass@kubelab.live, secret}`, and the seed, the rotation, the access review and the reconciler all follow it. Done when, in each env and **before** #1825 reaches it, `make grafana-admin-reconcile ENV=<env>` has renamed row id 1, and `CHECK=1` then exits 0. `make break-glass SVC=grafana` must log in as `breakglass`, and `make auth-review` must show it `OK` rather than `UNDECLARED`.
- [ ] [AC7] `toolkit auth drill-idp-down --env staging`: scale Authelia to 0, open each declared path, restore in a `finally`, and report `measured=False` if Argo CD restores Authelia mid-drill (#1083).
- [ ] [AC7] Exercise the break-glass path once: a local-password login to one service while OIDC is unavailable, per ADR-062 D4. Document it in a runbook as exceptional. A break-glass path that has never been used is a hypothesis.
- [ ] [AC1] After merge, apply to prod and repeat the live authentication evidence there. Gitea's identity environment is prod (`gitea_identity_env`), so its check is not a staging one.

## Closing

- [ ] Every acceptance criterion from `proposal.md` is covered by at least one test
- [ ] Every acceptance criterion has a matching entry in `features.json` with a non-vacuous verification command
- [ ] Type checks pass
- [ ] `make test` green, and `make test-infra ENV=staging` shows no new failures
- [ ] Any gotcha that outlived the change is in CLAUDE.md or `docs/lessons.md`
- [ ] Board ticket #1013 reflects reality
