[
  {
    "id": "TOOL-016-ansible-transport-f1",
    "behavior": "transport=bastion adds a ProxyCommand (SSOT-derived) to every mesh-only node and none to the public-IP VPS",
    "verification": "poetry run pytest tests/test_generator_ansible.py -k bastion -q",
    "state": "verified",
    "evidence": "2026-07-10: pytest -k bastion → 2 passed. Smoke (real generate --transport bastion): ace2 & aws1 carry `ProxyCommand=ssh -i ~/.ssh/id_ed25519 -W %h:%p -q ... deployer@<vps.public_ip>`; kubelab-vps has no per-host args (inherits global, jump-free)."
  },
  {
    "id": "TOOL-016-ansible-transport-f2",
    "behavior": "Default / transport=mesh reproduces the current inventory unchanged (regression guard)",
    "verification": "poetry run pytest tests/test_generator_ansible.py -k mesh -q",
    "state": "verified",
    "evidence": "2026-08-09 (re-run after rebase onto master): pytest -k mesh → 4 passed, 1 deselected. The three TestMeshTransportRegression cases (no per-host ssh args in mesh; global args unchanged; mesh is the default) plus test_mesh_only_nodes_get_proxy_via_ssot_bastion, whose name carries both tokens — -k substring-matches the full node ID, class name included. The 2026-07-10 entry recorded 3, which does not reproduce against the committed test names under any pytest version; the branch is a single squashed commit, so whether that was a transcription of the mesh class's size or a rename landing after the capture cannot be recovered. Full non-e2e suite: 394 passed, 0 failed."
  },
  {
    "id": "TOOL-016-ansible-transport-f3",
    "behavior": "The bastion target derives from networking.* SSOT — no public IP hardcoded in generator source",
    "verification": "! grep -REn '([0-9]{1,3}\\.){3}[0-9]{1,3}' toolkit/features/generator_ansible.py",
    "state": "verified",
    "evidence": "2026-07-10: grep finds 0 dotted-quad IPs in generator_ansible.py (exit 1). Bastion host/user/key derived from networking.vps.public_ip / ssh_users.cloud / networking.ssh_key."
  },
  {
    "id": "TOOL-016-ansible-transport-f4",
    "behavior": "CLI exposes --transport and the Makefile provision target threads TRANSPORT= into generate",
    "verification": "poetry run toolkit infra ansible generate --help | grep -q -- '--transport' && grep -q 'TRANSPORT' Makefile",
    "state": "verified",
    "evidence": "2026-07-10: `--transport` present in `ansible generate --help` (validated to mesh|bastion); Makefile provision regenerates via `--transport $(TRANSPORT)` then restores mesh."
  },
  {
    "id": "TOOL-016-ansible-transport-f5",
    "behavior": "The new AnsibleGenerator unit suite passes (backfills previously-absent coverage)",
    "verification": "poetry run pytest tests/test_generator_ansible.py -q",
    "state": "verified",
    "evidence": "2026-07-10: tests/test_generator_ansible.py → 5 passed. ruff clean, mypy clean on generator_ansible.py."
  },
  {
    "id": "TOOL-016-ansible-transport-f6",
    "behavior": "End-to-end: make provision TRANSPORT=bastion reaches a MESH-ONLY node from an OFF-MESH Linux controller through the VPS bastion (RUNTIME, off-mesh-gated)",
    "verification": "make provision NODE=aws1 ENV=prod TRANSPORT=bastion CHECK=1 TAGS=transport-probe-only",
    "state": "verified",
    "evidence": "2026-08-09 REAL RUN, controller verifiably OFF the mesh. Off-mesh precondition established BEFORE the run, not assumed: `tailscale status` → 'Tailscale is stopped'; tailscale0 carried only a link-local fe80::/64 and no 100.64.0.1; `ping -c1 100.64.0.7` (aws1) unreachable; while `getent hosts vpn.kubelab.live` still returned the public 162.55.57.175, so the bastion stayed reachable exactly as designed. Result: exit 0, PLAY RECAP `ok=3 changed=0 unreachable=0 failed=0 skipped=2`, 35.6s wall, `TASK [Gathering Facts] ok: [aws1]`. Inventory auto-restored by the target's own restore leg — 0 ProxyCommand remaining, aws1 back to ansible_host aws1.kubelab.internal. Non-invasive by construction: `--check` plus `--tags transport-probe-only`, a tag NO task in the tree carries, so the play reduces to its connection phase and executes zero tasks. This supersedes the 2026-08-09 on-mesh dress rehearsal (exit 0, ok=3, 34s), which proved the bastion path and the VPS-side MagicDNS resolution of aws1.kubelab.internal but could not prove the claim f6 actually makes, because a mesh route existed at the time."
  }
]
