---
tags: [spec, tasks, templates]
created: "2026-08-27"
---

# Tasks - TOOL-035

> TDD order. One task = one focused commit. Tick as you go. Reorder freely while spec is in `draft` state; freeze once you start `implementing`.
>
> **Inline markers**: `[P]` — no dependency on another unchecked task; `[AC<n>]` — helps satisfy acceptance criterion `<n>` in `proposal.md` (lets `/spec check` map coverage deterministically).
>
> **Shape**: four atomic PRs, each ≤300 LOC. PR0 is a precondition of the first push and is independent of everything else. PR1 → PR2 → PR3 is a real chain: nothing migrates into an organization that does not exist, and no workflow runs on a repository that has not arrived.

## Setup

- [x] Branch created from master: `feat/tool-035-gitea-repository-reconciliation` (worktree `~/Projects/kubelab-gitea-pilot-wt`) ✓ 2026-08-27
- [ ] `proposal.md` is complete and acceptance criteria are testable
- [x] **Risk 1 resolved — who creates the organization.** ✓ 2026-08-27, measured with a throwaway org created and deleted in one run (transcript in `verification.md`). **The superadmin creates organizations; the bot creates repositories inside them.** The contradiction was apparent, not real: `write:organization` is permission to *act on* organizations, not ownership of them, and the two live at different layers — the token scope refuses before org membership is ever consulted. Two residuals carried into PR1 rather than closed: (a) the team-permission half is **unproven**, because both bot calls were refused at the scope layer and the team's permission was never exercised — a 403 from a missing scope and one from a missing permission are the same status code, the trap AUTH-004 AC5 already hit once; (b) creating a team with an explicit `units` list and `permission: "write"` returned `permission: none`, so the reconciler must **read the team back and assert its effective permission**, never trust the request body.
- [x] **Risk 2 — the GitHub credential for migration.** ✓ 2026-08-28. Minted by the operator as `gitea-migration`, **60 days** (expires 2026-10-27), granted **`resume` and `fae-brain`** — broader on both axes than the 30-day/one-repo proposal below, deliberately, so the second ADR-065 pilot needs no reissue. Stored at `apps.services.core.gitea.github_migration_token` in `prod.enc.yaml`, registered in `SECRET_CATALOG` as `EXTERNAL` / `Expiry.PROVIDER`, and wired into `PROVIDER_CHECKS`. **Verified by consequence, both halves** (transcript in `verification.md`): granted private repos answer 200, `knowledge` answers **404** — a fine-grained PAT refuses with 404 rather than 403, because GitHub does not confirm existence to an unauthorised caller, and reading that as "the repo is gone" is the wrong lesson. A write on a *granted* repo is refused 403, so Read-only holds rather than merely being unexercised. Original proposal, kept for the record:
- [ ] ~~PROPOSED, awaiting the operator; blocks PR2 only, not PR1.~~ Fine-grained PAT on `mlorentedev/resume` alone, permissions **Contents: Read, Metadata: Read, Issues: Read, Pull requests: Read** — no write anywhere, since the migration only reads from GitHub and Gitea writes on its own side. Proposed expiry **30 days**: it covers the pilot window for all three ADR-065 repositories and then dies on its own, and a migration credential that outlives its migration is the exposure SEC-GITEA-001 just closed. Scope stays at one repository because the other two are not being migrated yet, and fine-grained PATs can be edited to add repositories without reissuing. Only the operator can mint it (browser flow, their account); the value goes to SOPS by stdin and never through a transcript.
- [x] **Risk 3 — which forge accepts a merge during the pilot.** ✓ 2026-08-28, operator approved the recommendation below. **Gitea accepts merges from the migration onward; GitHub accepts none and is the frozen rollback snapshot.** Measured 2026-08-27: nothing blocks merging on GitHub today — four of the five PRs are `MERGEABLE/UNSTABLE` (checks red but not required) and branch protection does not exist on this repository at all (`403: Upgrade to GitHub Pro or make this repository public`). So draining on GitHub first is *available*; it is just worse, because it merges four changes with no CI. Migrating first and merging in Gitea drains them **verified** instead, and D4's rollback survives untouched — the GitHub copy keeps all five open. Rests on one assumption to verify in PR2 rather than assume: that migrated pull requests arrive mergeable. All five are same-repo branches, so the heads come with the clone. If they do not, fall back to draining on GitHub with the local `make check` path that `resume`'s own PR #255 runbook documents.

## Implementation

### PR0 — the anonymous surface is guarded live (Risk 5, closes #1389 AC1/AC4; AC2 partial)

> **Ships on its own branch, against #1389 — not on this spec's branch.** It is sequenced here because
> it is a precondition of the first push, not because it is TOOL-035's scope. Every other PR below is
> this spec's.
>
> **Opened as [#1491](https://github.com/mlorentedev/kubelab/pull/1491)** on branch
> `fix/sec-gitea-001-live-anonymous-guard` (worktree `~/Projects/kubelab-gitea-hardening-wt`).
> Evidence for all three criteria is in [this comment](https://github.com/mlorentedev/kubelab/issues/1389#issuecomment-5447690725).

- [x] [P] Write a failing live check in `tests/infra/`: anonymous `GET` of `/explore`, `/explore/users`, `/api/swagger` and `/api/v1/version` against the running Gitea must be refused. Read the host from the SSOT, never a literal. Demonstrate it can go red — the existing `tests/test_gitea_anonymous_surface.py` asserts on the *rendered template*, and the prod e2e suite probes only `/api/healthz`, which `REQUIRE_SIGNIN_VIEW` exempts by design (6/6 green 2026-08-27, and that greenness says nothing about anonymous exposure) ✓ 2026-08-27 — `tests/infra/test_gitea_anonymous_surface_live.py`, 5/5. `/explore` dropped from the list: it answered 303 *before* the fix too, so asserting on it would pass without the hardening. `/api/healthz` kept as a **control** — it must stay 200, so a bare `!= 200` assertion would fail on it, which is how the suite refuses to pass against a forge that is merely down
- [x] Run it against prod; if red, `make provision NODE=bee ENV=prod` and re-run. Paste both transcripts in `verification.md`. **`NODE=bee`, not `beelink`** — the playbook is `provision-bee.yml` while the `common.yaml` node key is `beelink`, and `make provision` interpolates the playbook name, so the node key fails with "Inventory not found". A fresh worktree also needs `toolkit infra ansible generate --env prod` first: generated inventories are gitignored and therefore per-worktree ✓ 2026-08-27. All four closed paths answered **200** on 2026-08-24 and are refused now. Re-run 2026-08-31 against live prod: still 5/5
- [ ] Prove the hardening did not close the path the migration needs: an authenticated `git clone` over HTTPS with `hefesto`'s token still succeeds (#1389 AC2). This doubles as the token liveness check — **PARTIAL, deliberately not ticked.** The *contrast* is measured (`/api/v1/version` and `/api/v1/user`: 403 anonymous, 200 authenticated as `hefesto`, owning 0 repositories), which proves the refusal is about authentication rather than the API having been switched off. AC2's literal `git clone` could not be run: **the forge holds no repository to clone**. That evidence arrives with PR2, when `resume` lands
- [x] Confirm the provision is idempotent (`changed=0` on re-run) (#1389 AC4) ✓ 2026-08-27 — run 1 `ok=128 changed=7`, run 2 `ok=127 changed=0`. The seven first-run changes were accumulated drift on the node, not a defect in the role: the Beelink had not been provisioned since some template changes landed. That drift is itself the argument for a live probe — a merged template is not a deployed one

### PR1 — organizations and repositories are declared and reconciled (AC1, AC2, AC4)

- [x] [P] [AC1] Write failing test: a declaration fixture of one org + one repo produces the create calls; a second run against a state where both exist produces none ✓ 2026-08-27 — `tests/test_gitea_repo_reconcile.py`, red first with `ModuleNotFoundError`
- [x] [P] [AC2] Write failing test: a repository present in the API response and absent from the declaration is **reported** and appears in no delete call. Assert there is no delete path at all, not merely that it went unused ✓ 2026-08-27 — `test_the_plan_has_no_deletion_field` asserts on `dataclasses.fields(ReconcilePlan)`, so it holds for every future caller rather than for the fixtures that happen to exist. Discrimination demonstrated against a control dataclass carrying `repos_to_delete`: guard flags the control, passes the real plan
- [x] [AC1] Add the `gitea.organizations` declaration to `common.yaml` per ADR-065 D2 — `teledyne` (`fae-brain`, `openkm-brain`), `personal` (`resume`), `kubelab` declared empty per D3 ✓ 2026-08-27. Two tests tie the declaration to the ADR rather than to a comment: `test_the_real_declaration_matches_adr_065` reads the live file through the same `load_declaration` the reconciler uses, and `test_the_vault_is_not_declared` fails if `knowledge` is ever added — a one-line edit with no other tell
- [x] [AC1] [AC2] [AC4] Implement the planning half in `toolkit/features/gitea_repos.py` ✓ 2026-08-27 — 13 tests green, lint clean. **Deliberately split**: `plan_reconcile` is pure (two sets in, a plan out, no network) so the safety properties are structural rather than behavioural. The I/O half (`execute`, the Gitea client, the `make` target) is still to write, and so is the team-permission read-back that Risk 1's second residual requires
- [x] [AC4] Write failing test asserting the reconciler never creates an organization under the bot's ownership, per Risk 1's settled answer ✓ 2026-08-27 — `actor_for_org_creation() is Actor.SUPERADMIN`, with its counterpart for repositories. Encoded as a function rather than a comment because ADR-065 D1 makes it an acceptance criterion, not a style choice
- [x] [AC1] [AC2] Implement the I/O half: a Gitea client, `execute(plan)`, and the `make` target. `ensure_team` must **read the created team back** and assert its effective permission — Risk 1 measured `permission: "write"` coming back as `permission: none` when `units` is passed ✓ 2026-08-31 — `toolkit/features/gitea_client.py` + `execute`/`ensure_team` in `gitea_repos.py`, `toolkit services gitea reconcile` / `make gitea-reconcile`, 13 tests. Three decisions worth naming: **reads use the ADMIN credential**, because the bot cannot see an organization it is not a member of and would report an existing private org as absent (lesson-408's corollary, and the reason the read path is not an afterthought); **pagination is exhaustive**, terminating on a short page rather than a total not every endpoint returns; and **failures are collected, not aborted on** — `execute` already quarantines the cascade via `failed_orgs`, the reconciler is idempotent so a re-run recovers, and a first run with a mis-scoped token refuses every organization, where stopping at the first would report one problem instead of five. The command is **plan-only by default**; `--apply` is opt-in
- [x] [AC1] [AC4] Both credentials are minted declaratively rather than by hand ✓ 2026-08-31 — `gitea_bot_scopes` widened with `write:organization` (Risk 1's residual: the bot was refused `POST /orgs/<org>/repos` with `required=[write:organization]`), and new tasks mint the superadmin's `admin_token` by the same mechanism as the machine token. **Gitea does not allow a token's scopes to be edited after minting and the mint tasks are gated on the SOPS key being absent**, so widening the string alone changes nothing on an instance that already holds a token — the old one must be revoked and its key deleted first. Recorded in the variable's comment, where whoever tries it will be looking
- [ ] [AC1] [AC2] [AC4] Live reconcile transcript in `verification.md`: first run creates, second reports no change, an undeclared repository is reported and survives, and `hefesto` owns nothing afterwards

### PR2 — `resume` migrates with its issues and pull requests (AC3, AC5)

- [ ] [P] [AC5] Write failing check: the GitHub migration credential answers an authenticated call with 200 **and** is refused one outside its scope. Both halves — a token that works proves only half the claim (AUTH-004 AC5's finding)
- [ ] [AC5] Store it in SOPS, register it in `SECRET_CATALOG`, deliver it by the same path as every other secret. Never print it
- [ ] [AC3] Extend the reconciler to drive Gitea's migration endpoint with `issues`, `pull_requests`, `labels`, `milestones` and `releases` enabled
- [ ] [AC3] Migrate `resume`. Verify by counting through the API against the GitHub baseline — **28 open issues, 5 open pull requests**, all five with a same-repository head (`#253, #255, #256, #258, #259`) so every head branch must resolve. **The issue count was `20` here until 2026-08-31, and it was wrong**: it came from `gh issue list --limit 20`, a query structurally incapable of returning more, so the number measured the flag rather than the repository (lesson-408). Recounted by paginating to exhaustion via `gh api --paginate`, filtering out `pull_request` entries — the REST issues endpoint returns PRs as issues, so an unfiltered count would have overshot by five in the other direction. A baseline is inherited by every later check, so this one is recorded with the command that produced it
- [ ] [AC3] Record in `verification.md` what did **not** carry over, if anything, rather than reporting only the counts that matched

### PR3 — CI runs on the forge (AC6, AC7)

- [ ] [P] [AC6] **Actions enablement is two switches, and both are currently undeclared.** Instance-level: `GITEA__actions__ENABLED` is **absent from `compose.yml.j2`** (verified 2026-08-27 — the only `actions` matches in that file are comments about the *GitHub* runner), so it rides on the image default. Per-repo: the repository's Actions unit, which a migration does not necessarily turn on. Declare both — the instance switch in the compose template, the per-repo unit in the reconciler's declaration — rather than toggling either by hand (ADR-065 D3: declared, not remembered). Verify the live default before assuming which way it falls. Miss either switch and a push runs nothing and reports no error, which is the fails-open shape, not a visible failure
- [ ] [P] [AC7] Write failing test for the `act_runner` role: registration is declarative, resource limits follow ADR-030, and the label mapping for `runs-on: ubuntu-latest` exists (it does not exist by default)
- [ ] [AC7] Implement the role in `infra/ansible/roles/`, deploy to the Beelink, confirm `changed=0` on re-run
- [ ] [AC6] Push to `resume` in Gitea and get `ci.yml`'s six jobs green. The three known friction points from Risk 4, in the order they bite: the Docker socket for `setup-buildx-action`/`build-push-action` (four of six jobs build images — ADR-030's existing Beelink GitHub runner is the precedent), `actions/upload-artifact@v4` against Gitea 1.25.5's v4 backend, and the `ubuntu-latest` label mapping
- [ ] [AC6] Record the three non-portable workflows (`release.yml`, `add-to-project.yml`, `bitacora-status.yml`) as an accepted loss with what replaces them or the explicit decision that nothing does. A silent drop is the failure mode this criterion exists to prevent

## Closing

- [ ] Every acceptance criterion from `proposal.md` is covered by at least one test
- [ ] Every acceptance criterion has a matching entry in `features.json` with a non-vacuous verification command
- [ ] Type checks pass
- [ ] Lint passes
- [ ] `make test` green, and `make test-infra ENV=prod` shows no new failures
- [ ] No unrelated changes in the diff (no scope creep)
- [ ] `verification.md` filled in
- [ ] Any gotcha that outlived the change is in CLAUDE.md or `docs/lessons/`
- [ ] Board tickets reflect reality: #1076 and #1389 closed with evidence; #504 answered by construction and closed with a pointer here; #1077's step [3] ticked
- [ ] PRs opened referencing this spec folder; `adversarial-review` before `/spec archive`
